#pragma once

#include "../utils/strong_typedef.h"
#include <string>
#include <GL/glew.h>

using FilePath = strong_typedef<std::string, class FilePathTag>;

using GLUid = strong_typedef<GLuint, class GLuintTag>;

